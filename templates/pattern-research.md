Goal:
Answer <question> well enough to decide <decision>.

Why now:
<the decision waiting on it, and when.>

## Scope
Find it, cite it, recommend.
Already known, do not re-litigate: <...>

Out of scope: building anything; committing to the recommendation.

## Completion criteria
A findings note with sources, an explicit list of what could NOT be established, and one
recommendation with its main risk named.

## Evidence
Every claim carries a source URL. Anything unsourced is labelled as inference, in line.

## Overrides
* No code changes, no repo work — this task writes to outputs/ only.
* Web search is required. <Remove this line if the project config already allows it.>

Note: a complete report does not authorise the project it recommends. That is a separate card.
