# secrets — never commit
.env
.env.local
*.key

# macOS
.DS_Store

# temp files from atomic writes (write .tmp -> mv)
*.tmp

# logs
logs/**/*.log

# local runner coordination state
state/runner.sqlite
state/runner.sqlite-journal
state/runner.sqlite-shm
state/runner.sqlite-wal
